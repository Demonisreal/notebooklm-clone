PORT=3001
WEB_ORIGIN=http://localhost:3000

SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
SUPABASE_SECRET_KEY=sb_secret_...
SUPABASE_JWT_ISSUER=http://127.0.0.1:54321/auth/v1

# leave empty; once set, this account can no longer create or delete anything
DEMO_USER_EMAIL=
# gemini budget for that account: per client address per hour, and one cap per day for all of them
DEMO_CHAT_PER_HOUR=10
DEMO_STUDIO_PER_HOUR=3
DEMO_DAILY_CAP=200

# fake runs without an api key and returns deterministic embeddings
LLM_PROVIDER=fake
GEMINI_API_KEY=
GEMINI_CHAT_MODEL=gemini-3.5-flash
GEMINI_EMBEDDING_MODEL=gemini-embedding-2
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts

